# ATAI Bot - Backend (Part 1: Core)

This folder contains the core FastAPI backend skeleton for the ATAI Bot.

## Quickstart (local)

1. Create Python venv:
   ```bash
   python -m venv .venv
   source .venv/bin/activate    # Linux / macOS
   .venv\Scripts\activate       # Windows PowerShell
